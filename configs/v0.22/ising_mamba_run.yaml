# v0.22 LatticeMamba - Quick run for HPO (10 epochs)
# Use with *_opt.yaml for hyperparameter search
# batch_size × num_beta × accumulation_steps = 64 × 8 × 32 = 16,384 samples/epoch

project: Ising_VaTD_v0.22
device: cuda:0
net: model_mamba.LatticeMamba
optimizer: splus.SPlus
scheduler: hyperbolic_lr.ExpHyperbolicLR
epochs: 10
batch_size: 64
seeds: [42]

net_config:
  # Lattice configuration
  size: 16
  fix_first: 1
  category: 2

  # Temperature sampling
  batch_size: 64
  num_beta: 8
  beta_min: 0.1
  beta_max: 2.0

  # Training Mode
  training_mode: sequential
  accumulation_steps: 32

  mcmc_enabled: false
  cudnn_benchmark: true
  curriculum_enabled: false

  # Mamba architecture (small width for HPO)
  d_model: 64
  n_layers: 4
  d_state: 32
  expand_factor: 2
  d_conv: 4
  cond_dim: 64
  pscan: true

  # Temperature-dependent Output Scaling
  logit_temp_scale: true
  temp_scale_min: 0.1
  temp_scale_max: 2.0

  # Zero-Variance Effective Action Loss
  alpha_zv: 0.01

  # Adaptive sampling disabled for HPO runs
  adaptive_sampling_enabled: false
  adaptive_sampling_warmup: 3
  adaptive_sampling_num_bins: 32
  adaptive_sampling_kappa: 1.0
  adaptive_sampling_epsilon: 1.0e-6
  adaptive_sampling_momentum: 0.95

optimizer_config:
  lr: 1.e-1
  eps: 1.e-10

scheduler_config:
  upper_bound: 300
  max_iter: 150
  infimum_lr: 1.e-6

early_stopping_config:
  enabled: false
